#!/bin/bash啊啊啊啊啊
#
#
#
/etc/rc.d/init.d/mongodb-16730
# init script for mongodb-16730 precesses
# processname: mongodb-16730
# description: mongodb, port： 16730
# chkconfig: 2345 86 16
# description: mongodb-16730

if [ -f /etc/init.d/functions ]; then
      . /etc/init.d/functions
elif [ -f /etc/rc.d/init.d/functions ]; then
      . /etc/rc.d/init.d/functions
else
      echo -e "/atomcat: unable to locate functions lib. Cannot continue."
      exit -1
fi

RETVAL=$?

case $1 in
start)
     su pmctestmaster /mongo/mongodata-16730/bin/dbstartup.sh
;;

stop)
     su pmctestmaster /mongo/mongodata-16730/bin/dbshutdown.sh
;;

*)
     echo $"Usage: $0 {start|stop}"
     exit 1
;;
esac

exit $RETVAL